{
  "name": "circuits",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "circomlib": "0.5.5",
    "hardhat": "^2.2.0",
    "hardhat-circom": "^2.0.0"
  },
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "circom:dev": "hardhat circom --deterministic --debug --verbose",
    "circom:prod": "hardhat circom --verbose"
  }
}
